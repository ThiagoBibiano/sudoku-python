# Contribuindo

Obrigado por contribuir! 🎉

1. Crie uma branch a partir de `developer`:
   - `feature/...` para novas funcionalidades
   - `fix/...` para correções
   - `docs/...` para documentação

2. Faça commits pequenos e descritivos:
   - Ex.: `feat(core): add Board container class`

3. Abra um Pull Request para `developer`.  
   - Ao concluir um bloco de etapas, `developer` será integrado em `main`.

4. Siga estilo:
   - PEP8 (use Black/Ruff se possível)
   - Docstrings no formato Google Style
   - Código orientado a objetos e modular.

